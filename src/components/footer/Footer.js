const styles = {
    backgroundColor: 'red',
    position: 'fixed',
    bottom: '0',
    width: '100%',
};

export function Footer() {
    return(
        <div className="footer" style={styles}>
            Footer works!
        </div>
    );
}
